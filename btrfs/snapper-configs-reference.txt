# ==============================================================================
# BTRFS & SNAPPER CONFIGURATION REFERENCE
# ==============================================================================
# Machine: CachyOS Btrfs on NVMe
# Subvolumes:
#   ID 256 path @              (/)
#   ID 257 path @home          (/home)
#   ID 258 path @root          (/root)
#   ID 259 path @srv           (/srv)
#   ID 260 path @cache         (/var/cache)
#   ID 261 path @tmp           (/var/tmp)
#   ID 262 path @log           (/var/log)
#   ID 486 path @snapshots     (/.snapshots)
#   ID 639 path @home_snapshots(/home/.snapshots)
#
# Snapper Configs:
# 1. Config 'root' -> /
#    NUMBER_LIMIT: 6, NUMBER_LIMIT_IMPORTANT: 6, NUMBER_MIN_AGE: 1800
#    TIMELINE_CREATE: no, TIMELINE_CLEANUP: yes
# 2. Config 'home' -> /home
#    NUMBER_LIMIT: 6, NUMBER_LIMIT_IMPORTANT: 6, NUMBER_MIN_AGE: 3600
#    TIMELINE_CREATE: no, TIMELINE_CLEANUP: yes
#
# To recreate Snapper configs on a fresh installation:
#   sudo snapper -c root create-config /
#   sudo snapper -c home create-config /home
#   sudo systemctl enable --now snapper-cleanup.timer
# ==============================================================================
